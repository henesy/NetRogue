The encPlayer encoder will create and encode a character, hopefully locking the player sprite with a passcode/pin for loading once or if needed by the server.

This is to be run prior to server operation.
